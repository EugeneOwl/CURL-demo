{% extends "base.html.twig" %}

{% block contents %}
    {{ form_start(searchForm) }}
    {{ form_row(searchForm.name) }}

    {{ form_end(searchForm) }}
    {{ weatherShortDescription|raw }}
    <p>
        <a href="{{ path('app_login') }}">Log in</a>
    </p>
    <p>
        <a href="{{ path('app_registration') }}">Log up</a>
    </p>
    <p>
        <a href="{{ path('app_stealer') }}">Stealer page</a>
    </p>
{% endblock %}